/*
 * File:   header.h
 * Author: Igor da Rosa 
 *
 * 
 */
#ifndef HEADER_H
#define HEADER_H

#include <xc.h> //include padrão
#include <pic16f877a.h>//padrão do PIC
#include <stdio.h> //padrão do C

//inclusão de outros headers

//frequencia do cristal
#define _XTAL_FREQ 4000000

//definições
#define Delay __delay_ms
#define Bit __bit

//declaração de variaveis globais
//Bit flag;

//função padrão de inicialização
void inicializar()
{
    //configuração de inicialização
    OPTION_REG = 0b01111111; //configura o PIC
    
    //inicialização das portas
    TRISB = 255;//inputs 1111 1111 = 255
    TRISC = 0;//outputs 0000 0000 = 0
    //TRISD = ?

    //interrupções
    /*//OPTION_REGbits.INTEDG = 0;// ou 1
    INTCONbits.GIE = 1;
    INTCONbits.INTE = 1;*/
    
    //clear nas ports
    PORTB = 255;
    PORTC = 0;
    PORTD = 0;
};

//tratamento da interrupção
/*void __interrupt() TrataInt(void)
{
    if(INTF) //testar o flag
    {
        //INTCONbits.INTF = 0;
	flag = 1;
    }
    return;
};*/

//funções



#endif